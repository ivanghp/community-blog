@model IEnumerable<Activity.DataLayer.Entities.Post>

@{
    ViewData["Title"] = "GetByUser";
}

<h1>GetByUser</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Title)
            </th>
            @*<th>
                @Html.DisplayNameFor(model => model.Post.Description)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Post.Thumbnail)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Post.Location)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Post.Link)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Post.CreatedAt)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Post.CreatedBy)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Post.UpdatedAt)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Post.UpdatedBy)
            </th>*@
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Title)
            </td>
            @*<td>
                @Html.DisplayFor(modelItem => item.Post.Description)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Post.Thumbnail)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Post.Location)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Post.Link)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Post.CreatedAt)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Post.CreatedBy)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Post.UpdatedAt)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Post.UpdatedBy)
            </td>*@
            @*<td>
                <a asp-action="Edit" asp-route-id="@item.Id">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.Id">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.Id">Delete</a>
            </td>*@
        </tr>
}
    </tbody>
</table>
